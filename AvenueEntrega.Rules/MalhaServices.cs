using AvenueEntrega.Model;

namespace AvenueEntrega.Rules
{
    public static class MalhaServices
    {
        public static float EncontrarMelhorRota(this Mapa mapa, string nomeMapa, string origem, string destino, float autonomia)
        {
            //TODO: calculo da melhor roda por A*
            //saber se origem esta no mapa
            //saber se destino esta no mapa
            //saber se existe um trajeto ate o destino

            return 0.0f;
        }
    }
}
